{% extends 'base.html.twig' %}

{% block title %}Modifier un tour | {{ parent() }}{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-6">
                <h1>Modifier un tour</h1>
                <h3>{{ round.name }}</h3>
                {{ form_start(roundForm) }}
                {{ form_widget(roundForm) }}
                <button type="submit" id="btnSave" class="btn btn-secondary">Valider</button>
                <button onclick="history.back()" class="btn btn-secondary">Retour</button>
                {{ form_end(roundForm) }}
            </div>
        </div>
    </div>
{% endblock %}