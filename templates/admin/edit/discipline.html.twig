{% extends 'base.html.twig' %}

{% block title %}Modifier un discipline | {{ parent() }}{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-6">
                <h1>Modifier une discipline</h1>
                <h3>{{ discipline.name }}</h3>
                {{ form_start(disciplineForm) }}
                {{ form_widget(disciplineForm) }}
                <button type="submit" id="btnSave" class="btn btn-secondary">Valider</button>
                <button onclick="history.back()" class="btn btn-secondary">Retour</button>
                {{ form_end(disciplineForm) }}
            </div>
        </div>
    </div>
{% endblock %}