{% extends 'base.html.twig' %}

{% block title %}Teams !{% endblock %}
{% block body %}
<table class="table mb-0 mt-4">
    <thead>
    <tr>
        <th scope="col">#</th>
        <th scope="col">Name</th>
        <th scope="col">Actions</th>
    </tr>
    </thead>
    <tbody>
    {% set i = 1 %}
    {% for team  in teams %}

        <tr>
            <th scope="row">{{ i }}</th>
            <td>{{ team.name }}</td>
            <td>
                <button>
                    <a href="{{ path('admin_edit_team', {'id': team.id}) }}">Edit</a>
                </button>
                <button>
                    <a href="#">Delete</a>
                </button>
            </td>
        </tr>
        {% set i = i+1 %}
    {% endfor %}
    </tbody>
</table>
<div class="text-center">
    <a class="btn btn-primary" href="{{ path('admin_home') }}">Back</a>
</div>

{% endblock %}